#include "main.h"
/**
 * mul -  function that multiplies two integers.
 * @u: first number that will run the program
 * @x: second number that will be tested
 * Return: 0
 */
int mul(int u, int x)
{
	int product;

	product = u * x
	return (product);
}

